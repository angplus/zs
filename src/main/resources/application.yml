bur:
  datasource:
    url: jdbc:mysql://192.168.1.17/BUR_PDA
    username: buradmin
    password: .buradmin
    driverClassName: com.mysql.jdbc.Driver
    test-on-borrow: true
    test-while-idle: true
    validation-query: select 1;

kt:
  datasource:
    url: jdbc:mysql://192.168.1.13/kt?useSSL=false&requireSSL=false
    username: kt
    password: hb20ngv5khfh
    driverClassName: com.mysql.jdbc.Driver
    test-on-borrow: true
    test-while-idle: true
    validation-query: select 1;

sa:
  datasource:
    url: jdbc:sqlserver://192.168.1.89;databaseName=KangTai
    username: sa
    password: sa_2013
    driverClassName: com.microsoft.sqlserver.jdbc.SQLServerDriver
    test-on-borrow: true
    test-while-idle: true
    validation-query: select 1;

spring:
  datasource:
    tomcat:
      test-on-borrow: true
      test-while-idle: true
      validation-query: select 1;
#  jpa:
#    properties:
#      hibernate:
#        show_sql: true
#        use_sql_comments: true
#        format_sql: true

server:
  port: 8080
  session:
    tracking-mode: cookie

#logging:
#  file: log.txt

#spring:
#  profiles:
#    active: production


#spring:
#  devtools:
#    livereload:
#      enabled: true
#    restart:
#      enabled: true



#  data:
#    rest:
#      base-path: /api
